Create whl with the following commands:

    pip install -r requirements_dev.txt
    
    python -m build

find the whl in the dist folder

run the installed program with:

    python -m manage migrate

    python -m manage runserver
